global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'routee-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['app:8080']   # dev compose 네트워크상 app 서비스명
